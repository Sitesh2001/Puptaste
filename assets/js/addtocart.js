let cardbtn = document.getElementsByClassName("card-action-btn");
let btnBatch = document.querySelector(".btn-badge")

cardbtn[0].addEventListener("click",()=>{
    let cartvalue = Number(btnBatch.innerText)
    btnBatch.innerText = cartvalue + 1;
})

cardbtn[1].addEventListener("click",()=>{
    let cartvalue = Number(btnBatch.innerText)
    btnBatch.innerText = cartvalue + 1;
})

cardbtn[2].addEventListener("click",()=>{
    let cartvalue = Number(btnBatch.innerText)
    btnBatch.innerText = cartvalue + 1;
})

cardbtn[3].addEventListener("click",()=>{
    let cartvalue = Number(btnBatch.innerText)
    btnBatch.innerText = cartvalue + 1;
})

cardbtn[4].addEventListener("click",()=>{
    let cartvalue = Number(btnBatch.innerText)
    btnBatch.innerText = cartvalue + 1;
})

cardbtn[5].addEventListener("click",()=>{
    let cartvalue = Number(btnBatch.innerText)
    btnBatch.innerText = cartvalue + 1;
})

cardbtn[6].addEventListener("click",()=>{
    let cartvalue = Number(btnBatch.innerText)
    btnBatch.innerText = cartvalue + 1;
})

cardbtn[7].addEventListener("click",()=>{
    let cartvalue = Number(btnBatch.innerText)
    btnBatch.innerText = cartvalue + 1;
})